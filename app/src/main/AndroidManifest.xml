<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="aut.bme.sportsdbandroidclient">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.SportsDBAndroidClient">
        <activity android:name=".ui.result.ResultActivity"></activity>
        <activity
            android:name=".ui.results.ResultsActivity"
            android:label="@string/title_activity_results"
            android:theme="@style/Theme.SportsDBAndroidClient.NoActionBar" />
        <activity
            android:name=".ui.leagues.LeaguesActivity"
            android:label="@string/title_activity_leagues"
            android:theme="@style/Theme.SportsDBAndroidClient.NoActionBar" />
    </application>

</manifest>